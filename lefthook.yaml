pre-commit:
  piped: true
  commands:
    format:
      priority: 1
      run: |
        pnpm prettier {staged_files} --write
      stage_fixed: true

    lint:
      priority: 2
      run: |
        pnpm eslint {staged_files} --max-warnings 0
      stage_fixed: true
